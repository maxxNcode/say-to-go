# Mapillary API Access Token (for Vercel serverless functions)
# Get one at: https://www.mapillary.com/dashboard/developers
# Add as MAPILLARY_TOKEN in Vercel environment variables (not in .env)
# The token is served to the client via api/mapillary-token.ts proxy
# MAPILLARY_TOKEN=MLY|your_token_here
